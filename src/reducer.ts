const ACTIONS = {
  LOGIN: 'LOGIN',
  LOGOUT: 'LOGOUT',
}
export const LOGIN = 'LOGIN'

export const reducer = (state: any, action: { type: string; booking?: any; company?: any }) => {
  switch (action.type) {
    default:
      return state
  }
}
